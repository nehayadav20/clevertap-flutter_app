import 'package:flutter/material.dart';
class AppInboxPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('App Inbox'),
      ),
      body: Center(
        child: Text('This is the App Inbox Page'),
      ),
    );
  }
}